# FastAPI 백엔드 origin. fe(axios)가 이 값을 baseURL 로 사용해 /api/* 호출.
# 비어있으면 fe 는 백엔드를 호출하지 않고 로컬 stub/Gemini 모드로 동작.
VITE_API_HOST=http://localhost:8000

# Gemini (Google AI Studio). 클라이언트에 포함되므로 프로덕션에서는 백엔드 프록시 권장.
VITE_GEMINI_API_KEY=
