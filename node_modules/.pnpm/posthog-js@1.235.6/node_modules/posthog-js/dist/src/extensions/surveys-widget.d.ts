import { PostHog } from '../posthog-core';
import { Survey } from '../posthog-surveys-types';
export declare function retrieveWidgetShadow(survey: Survey, posthog?: PostHog): ShadowRoot;
export declare function createWidgetStyle(widgetColor?: string): string;
